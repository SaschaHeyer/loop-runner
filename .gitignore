# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# node (landing page build)
node_modules/

# secrets & local config — never commit
.env
loop-runner/.env
.claude/settings.local.json

# runtime artifacts
cost_log.yaml
*.log
run.log

# os
.DS_Store

# Entire checkpoint metadata (local only — never publish conversation logs)
.entire/
