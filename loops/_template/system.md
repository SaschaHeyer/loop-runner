You are running one "__NAME__" loop. State the role, the scope, and the HARD boundaries here (what
this loop may and may not touch). Keep it short and durable — the per-loop task belongs in prompt.md,
not here. Delete this file (and the `system_prompt:` line in loop.yaml) if the loop doesn't need a
separate system prompt.
